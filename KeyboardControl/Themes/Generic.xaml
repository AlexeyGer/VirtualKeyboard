<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:KeyboardControl"
	xmlns:panel="clr-namespace:KeyboardControl.Panels">
	
	<Style TargetType="{x:Type local:Keyboard}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:Keyboard}">
					<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

						<ItemsControl ItemsSource="{Binding CharPadKeys, RelativeSource={RelativeSource AncestorType=local:Keyboard}}">
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
										<panel:KeyboardPanel />
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
						</ItemsControl>

						<!--<Grid Name="NumPadGrid"/>-->
						
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
